// Everything OK?
console.log('OK');

// Lightgallerys
$('.gallery').lightGallery({
    thumbnail: false,
    share: false,
    download: false,
    zoom: false,
    counter: false,
    autoplay: false,
    mode: 'lg-slide'
});
